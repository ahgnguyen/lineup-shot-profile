{
  "name": "lineup-shot-profile",
  "private": true,
  "description": "Root of an npm workspaces monorepo (backend, frontend). api/[...path].ts (the Vercel serverless entry point) resolves express/pg/d3-hexbin/typescript from backend's dependencies, hoisted here by npm workspaces.",
  "workspaces": [
    "backend",
    "frontend"
  ]
}
